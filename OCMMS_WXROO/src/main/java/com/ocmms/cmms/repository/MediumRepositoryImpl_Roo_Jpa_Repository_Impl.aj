// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.ocmms.cmms.repository;

import com.ocmms.cmms.model.pm.measuringpoint.Medium;
import com.ocmms.cmms.model.pm.measuringpoint.QMedium;
import com.ocmms.cmms.repository.MediumRepositoryImpl;
import com.querydsl.core.types.Path;
import com.querydsl.jpa.JPQLQuery;
import io.springlets.data.domain.GlobalSearch;
import io.springlets.data.jpa.repository.support.QueryDslRepositorySupportExt.AttributeMappingBuilder;
import java.util.List;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.transaction.annotation.Transactional;

privileged aspect MediumRepositoryImpl_Roo_Jpa_Repository_Impl {
    
    declare @type: MediumRepositoryImpl: @Transactional(readOnly = true);
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String MediumRepositoryImpl.CODE = "code";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String MediumRepositoryImpl.DESCRIPTION = "description";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String MediumRepositoryImpl.RECORD_STATUS = "recordStatus";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String MediumRepositoryImpl.CREATED_DATE = "createdDate";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String MediumRepositoryImpl.CREATED_BY = "createdBy";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String MediumRepositoryImpl.LAST_MODIFIED_DATE = "lastModifiedDate";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String MediumRepositoryImpl.LAST_MODIFIED_BY = "lastModifiedBy";
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param globalSearch
     * @param pageable
     * @return Page
     */
    public Page<Medium> MediumRepositoryImpl.findAll(GlobalSearch globalSearch, Pageable pageable) {
        
        QMedium medium = QMedium.medium;
        
        JPQLQuery<Medium> query = from(medium);
        
        Path<?>[] paths = new Path<?>[] {medium.code,medium.description,medium.recordStatus,medium.createdDate,medium.createdBy,medium.lastModifiedDate,medium.lastModifiedBy};        
        applyGlobalSearch(globalSearch, query, paths);
        
        AttributeMappingBuilder mapping = buildMapper()
			.map(CODE, medium.code)
			.map(DESCRIPTION, medium.description)
			.map(RECORD_STATUS, medium.recordStatus)
			.map(CREATED_DATE, medium.createdDate)
			.map(CREATED_BY, medium.createdBy)
			.map(LAST_MODIFIED_DATE, medium.lastModifiedDate)
			.map(LAST_MODIFIED_BY, medium.lastModifiedBy);
        
        applyPagination(pageable, query, mapping);
        applyOrderById(query);
        
        return loadPage(query, pageable, medium);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param ids
     * @param globalSearch
     * @param pageable
     * @return Page
     */
    public Page<Medium> MediumRepositoryImpl.findAllByIdsIn(List<Long> ids, GlobalSearch globalSearch, Pageable pageable) {
        
        QMedium medium = QMedium.medium;
        
        JPQLQuery<Medium> query = from(medium);
        
        Path<?>[] paths = new Path<?>[] {medium.code,medium.description,medium.recordStatus,medium.createdDate,medium.createdBy,medium.lastModifiedDate,medium.lastModifiedBy};        
        applyGlobalSearch(globalSearch, query, paths);
        
        // Also, filter by the provided ids
        query.where(medium.id.in(ids));
        
        AttributeMappingBuilder mapping = buildMapper()
			.map(CODE, medium.code)
			.map(DESCRIPTION, medium.description)
			.map(RECORD_STATUS, medium.recordStatus)
			.map(CREATED_DATE, medium.createdDate)
			.map(CREATED_BY, medium.createdBy)
			.map(LAST_MODIFIED_DATE, medium.lastModifiedDate)
			.map(LAST_MODIFIED_BY, medium.lastModifiedBy);
        
        applyPagination(pageable, query, mapping);
        applyOrderById(query);
        
        return loadPage(query, pageable, medium);
    }
    
}
