// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.ocmms.cmms.repository;

import com.ocmms.cmms.model.loto.PotentialHazard;
import com.ocmms.cmms.model.loto.QPotentialHazard;
import com.ocmms.cmms.repository.PotentialHazardRepositoryImpl;
import com.querydsl.core.types.Path;
import com.querydsl.jpa.JPQLQuery;
import io.springlets.data.domain.GlobalSearch;
import io.springlets.data.jpa.repository.support.QueryDslRepositorySupportExt.AttributeMappingBuilder;
import java.util.List;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PotentialHazardRepositoryImpl_Roo_Jpa_Repository_Impl {
    
    declare @type: PotentialHazardRepositoryImpl: @Transactional(readOnly = true);
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String PotentialHazardRepositoryImpl.CODE = "code";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String PotentialHazardRepositoryImpl.DESCRIPTION = "description";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String PotentialHazardRepositoryImpl.RECORD_STATUS = "recordStatus";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String PotentialHazardRepositoryImpl.CREATED_DATE = "createdDate";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String PotentialHazardRepositoryImpl.CREATED_BY = "createdBy";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String PotentialHazardRepositoryImpl.LAST_MODIFIED_DATE = "lastModifiedDate";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String PotentialHazardRepositoryImpl.LAST_MODIFIED_BY = "lastModifiedBy";
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param globalSearch
     * @param pageable
     * @return Page
     */
    public Page<PotentialHazard> PotentialHazardRepositoryImpl.findAll(GlobalSearch globalSearch, Pageable pageable) {
        
        QPotentialHazard potentialHazard = QPotentialHazard.potentialHazard;
        
        JPQLQuery<PotentialHazard> query = from(potentialHazard);
        
        Path<?>[] paths = new Path<?>[] {potentialHazard.code,potentialHazard.description,potentialHazard.recordStatus,potentialHazard.createdDate,potentialHazard.createdBy,potentialHazard.lastModifiedDate,potentialHazard.lastModifiedBy};        
        applyGlobalSearch(globalSearch, query, paths);
        
        AttributeMappingBuilder mapping = buildMapper()
			.map(CODE, potentialHazard.code)
			.map(DESCRIPTION, potentialHazard.description)
			.map(RECORD_STATUS, potentialHazard.recordStatus)
			.map(CREATED_DATE, potentialHazard.createdDate)
			.map(CREATED_BY, potentialHazard.createdBy)
			.map(LAST_MODIFIED_DATE, potentialHazard.lastModifiedDate)
			.map(LAST_MODIFIED_BY, potentialHazard.lastModifiedBy);
        
        applyPagination(pageable, query, mapping);
        applyOrderById(query);
        
        return loadPage(query, pageable, potentialHazard);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param ids
     * @param globalSearch
     * @param pageable
     * @return Page
     */
    public Page<PotentialHazard> PotentialHazardRepositoryImpl.findAllByIdsIn(List<Long> ids, GlobalSearch globalSearch, Pageable pageable) {
        
        QPotentialHazard potentialHazard = QPotentialHazard.potentialHazard;
        
        JPQLQuery<PotentialHazard> query = from(potentialHazard);
        
        Path<?>[] paths = new Path<?>[] {potentialHazard.code,potentialHazard.description,potentialHazard.recordStatus,potentialHazard.createdDate,potentialHazard.createdBy,potentialHazard.lastModifiedDate,potentialHazard.lastModifiedBy};        
        applyGlobalSearch(globalSearch, query, paths);
        
        // Also, filter by the provided ids
        query.where(potentialHazard.id.in(ids));
        
        AttributeMappingBuilder mapping = buildMapper()
			.map(CODE, potentialHazard.code)
			.map(DESCRIPTION, potentialHazard.description)
			.map(RECORD_STATUS, potentialHazard.recordStatus)
			.map(CREATED_DATE, potentialHazard.createdDate)
			.map(CREATED_BY, potentialHazard.createdBy)
			.map(LAST_MODIFIED_DATE, potentialHazard.lastModifiedDate)
			.map(LAST_MODIFIED_BY, potentialHazard.lastModifiedBy);
        
        applyPagination(pageable, query, mapping);
        applyOrderById(query);
        
        return loadPage(query, pageable, potentialHazard);
    }
    
}
