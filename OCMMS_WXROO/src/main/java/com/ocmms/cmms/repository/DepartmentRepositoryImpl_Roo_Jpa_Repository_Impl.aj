// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.ocmms.cmms.repository;

import com.ocmms.cmms.model.hrm.Department;
import com.ocmms.cmms.model.hrm.Organization;
import com.ocmms.cmms.model.hrm.QDepartment;
import com.ocmms.cmms.repository.DepartmentRepositoryImpl;
import com.querydsl.core.types.Path;
import com.querydsl.jpa.JPQLQuery;
import io.springlets.data.domain.GlobalSearch;
import io.springlets.data.jpa.repository.support.QueryDslRepositorySupportExt.AttributeMappingBuilder;
import java.util.List;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.transaction.annotation.Transactional;
import org.springframework.util.Assert;

privileged aspect DepartmentRepositoryImpl_Roo_Jpa_Repository_Impl {
    
    declare @type: DepartmentRepositoryImpl: @Transactional(readOnly = true);
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String DepartmentRepositoryImpl.CODE = "code";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String DepartmentRepositoryImpl.ORG_NAME = "orgName";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String DepartmentRepositoryImpl.DESCRIPTION = "description";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String DepartmentRepositoryImpl.POSTAL_CODE = "postalCode";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String DepartmentRepositoryImpl.PHONE = "phone";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String DepartmentRepositoryImpl.FAX = "fax";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String DepartmentRepositoryImpl.WEB = "web";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String DepartmentRepositoryImpl.MAIL = "mail";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String DepartmentRepositoryImpl.RECORD_STATUS = "recordStatus";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String DepartmentRepositoryImpl.CREATED_DATE = "createdDate";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String DepartmentRepositoryImpl.CREATED_BY = "createdBy";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String DepartmentRepositoryImpl.LAST_MODIFIED_DATE = "lastModifiedDate";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String DepartmentRepositoryImpl.LAST_MODIFIED_BY = "lastModifiedBy";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String DepartmentRepositoryImpl.ORGANIZATION_CATALOG = "organizationCatalog";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String DepartmentRepositoryImpl.ADDRESS = "address";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String DepartmentRepositoryImpl.ORGANIZATION = "organization";
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param globalSearch
     * @param pageable
     * @return Page
     */
    public Page<Department> DepartmentRepositoryImpl.findAll(GlobalSearch globalSearch, Pageable pageable) {
        
        QDepartment department = QDepartment.department;
        
        JPQLQuery<Department> query = from(department);
        
        Path<?>[] paths = new Path<?>[] {department.code,department.orgName,department.description,department.postalCode,department.phone,department.fax,department.web,department.mail,department.recordStatus,department.createdDate,department.createdBy,department.lastModifiedDate,department.lastModifiedBy,department.organizationCatalog,department.address,department.organization};        
        applyGlobalSearch(globalSearch, query, paths);
        
        AttributeMappingBuilder mapping = buildMapper()
			.map(CODE, department.code)
			.map(ORG_NAME, department.orgName)
			.map(DESCRIPTION, department.description)
			.map(POSTAL_CODE, department.postalCode)
			.map(PHONE, department.phone)
			.map(FAX, department.fax)
			.map(WEB, department.web)
			.map(MAIL, department.mail)
			.map(RECORD_STATUS, department.recordStatus)
			.map(CREATED_DATE, department.createdDate)
			.map(CREATED_BY, department.createdBy)
			.map(LAST_MODIFIED_DATE, department.lastModifiedDate)
			.map(LAST_MODIFIED_BY, department.lastModifiedBy)
			.map(ORGANIZATION_CATALOG, department.organizationCatalog)
			.map(ADDRESS, department.address)
			.map(ORGANIZATION, department.organization);
        
        applyPagination(pageable, query, mapping);
        applyOrderById(query);
        
        return loadPage(query, pageable, department);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param ids
     * @param globalSearch
     * @param pageable
     * @return Page
     */
    public Page<Department> DepartmentRepositoryImpl.findAllByIdsIn(List<Long> ids, GlobalSearch globalSearch, Pageable pageable) {
        
        QDepartment department = QDepartment.department;
        
        JPQLQuery<Department> query = from(department);
        
        Path<?>[] paths = new Path<?>[] {department.code,department.orgName,department.description,department.postalCode,department.phone,department.fax,department.web,department.mail,department.recordStatus,department.createdDate,department.createdBy,department.lastModifiedDate,department.lastModifiedBy,department.organizationCatalog,department.address,department.organization};        
        applyGlobalSearch(globalSearch, query, paths);
        
        // Also, filter by the provided ids
        query.where(department.id.in(ids));
        
        AttributeMappingBuilder mapping = buildMapper()
			.map(CODE, department.code)
			.map(ORG_NAME, department.orgName)
			.map(DESCRIPTION, department.description)
			.map(POSTAL_CODE, department.postalCode)
			.map(PHONE, department.phone)
			.map(FAX, department.fax)
			.map(WEB, department.web)
			.map(MAIL, department.mail)
			.map(RECORD_STATUS, department.recordStatus)
			.map(CREATED_DATE, department.createdDate)
			.map(CREATED_BY, department.createdBy)
			.map(LAST_MODIFIED_DATE, department.lastModifiedDate)
			.map(LAST_MODIFIED_BY, department.lastModifiedBy)
			.map(ORGANIZATION_CATALOG, department.organizationCatalog)
			.map(ADDRESS, department.address)
			.map(ORGANIZATION, department.organization);
        
        applyPagination(pageable, query, mapping);
        applyOrderById(query);
        
        return loadPage(query, pageable, department);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param organization
     * @param globalSearch
     * @param pageable
     * @return Page
     */
    public Page<Department> DepartmentRepositoryImpl.findByOrganization(Organization organization, GlobalSearch globalSearch, Pageable pageable) {
        
        QDepartment department = QDepartment.department;
        
        JPQLQuery<Department> query = from(department);
        
        Assert.notNull(organization, "organization is required");
        
        query.where(department.organization.eq(organization));
        Path<?>[] paths = new Path<?>[] {department.code,department.orgName,department.description,department.postalCode,department.phone,department.fax,department.web,department.mail,department.recordStatus,department.createdDate,department.createdBy,department.lastModifiedDate,department.lastModifiedBy,department.organizationCatalog,department.address,department.organization};        
        applyGlobalSearch(globalSearch, query, paths);
        
        AttributeMappingBuilder mapping = buildMapper()
			.map(CODE, department.code)
			.map(ORG_NAME, department.orgName)
			.map(DESCRIPTION, department.description)
			.map(POSTAL_CODE, department.postalCode)
			.map(PHONE, department.phone)
			.map(FAX, department.fax)
			.map(WEB, department.web)
			.map(MAIL, department.mail)
			.map(RECORD_STATUS, department.recordStatus)
			.map(CREATED_DATE, department.createdDate)
			.map(CREATED_BY, department.createdBy)
			.map(LAST_MODIFIED_DATE, department.lastModifiedDate)
			.map(LAST_MODIFIED_BY, department.lastModifiedBy)
			.map(ORGANIZATION_CATALOG, department.organizationCatalog)
			.map(ADDRESS, department.address)
			.map(ORGANIZATION, department.organization);
        
        applyPagination(pageable, query, mapping);
        applyOrderById(query);
        
        return loadPage(query, pageable, department);
    }
    
}
