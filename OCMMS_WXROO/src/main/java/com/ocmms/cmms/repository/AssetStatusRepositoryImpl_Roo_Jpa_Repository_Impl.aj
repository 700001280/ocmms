// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.ocmms.cmms.repository;

import com.ocmms.cmms.model.asset.AssetStatus;
import com.ocmms.cmms.model.asset.QAssetStatus;
import com.ocmms.cmms.repository.AssetStatusRepositoryImpl;
import com.querydsl.core.types.Path;
import com.querydsl.jpa.JPQLQuery;
import io.springlets.data.domain.GlobalSearch;
import io.springlets.data.jpa.repository.support.QueryDslRepositorySupportExt.AttributeMappingBuilder;
import java.util.List;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AssetStatusRepositoryImpl_Roo_Jpa_Repository_Impl {
    
    declare @type: AssetStatusRepositoryImpl: @Transactional(readOnly = true);
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String AssetStatusRepositoryImpl.CODE = "code";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String AssetStatusRepositoryImpl.DESCRIPTION = "description";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String AssetStatusRepositoryImpl.STATUS = "status";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String AssetStatusRepositoryImpl.RECORD_STATUS = "recordStatus";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String AssetStatusRepositoryImpl.CREATED_DATE = "createdDate";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String AssetStatusRepositoryImpl.CREATED_BY = "createdBy";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String AssetStatusRepositoryImpl.LAST_MODIFIED_DATE = "lastModifiedDate";
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    public static final String AssetStatusRepositoryImpl.LAST_MODIFIED_BY = "lastModifiedBy";
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param globalSearch
     * @param pageable
     * @return Page
     */
    public Page<AssetStatus> AssetStatusRepositoryImpl.findAll(GlobalSearch globalSearch, Pageable pageable) {
        
        QAssetStatus assetStatus = QAssetStatus.assetStatus;
        
        JPQLQuery<AssetStatus> query = from(assetStatus);
        
        Path<?>[] paths = new Path<?>[] {assetStatus.code,assetStatus.description,assetStatus.status,assetStatus.recordStatus,assetStatus.createdDate,assetStatus.createdBy,assetStatus.lastModifiedDate,assetStatus.lastModifiedBy};        
        applyGlobalSearch(globalSearch, query, paths);
        
        AttributeMappingBuilder mapping = buildMapper()
			.map(CODE, assetStatus.code)
			.map(DESCRIPTION, assetStatus.description)
			.map(STATUS, assetStatus.status)
			.map(RECORD_STATUS, assetStatus.recordStatus)
			.map(CREATED_DATE, assetStatus.createdDate)
			.map(CREATED_BY, assetStatus.createdBy)
			.map(LAST_MODIFIED_DATE, assetStatus.lastModifiedDate)
			.map(LAST_MODIFIED_BY, assetStatus.lastModifiedBy);
        
        applyPagination(pageable, query, mapping);
        applyOrderById(query);
        
        return loadPage(query, pageable, assetStatus);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param ids
     * @param globalSearch
     * @param pageable
     * @return Page
     */
    public Page<AssetStatus> AssetStatusRepositoryImpl.findAllByIdsIn(List<Long> ids, GlobalSearch globalSearch, Pageable pageable) {
        
        QAssetStatus assetStatus = QAssetStatus.assetStatus;
        
        JPQLQuery<AssetStatus> query = from(assetStatus);
        
        Path<?>[] paths = new Path<?>[] {assetStatus.code,assetStatus.description,assetStatus.status,assetStatus.recordStatus,assetStatus.createdDate,assetStatus.createdBy,assetStatus.lastModifiedDate,assetStatus.lastModifiedBy};        
        applyGlobalSearch(globalSearch, query, paths);
        
        // Also, filter by the provided ids
        query.where(assetStatus.id.in(ids));
        
        AttributeMappingBuilder mapping = buildMapper()
			.map(CODE, assetStatus.code)
			.map(DESCRIPTION, assetStatus.description)
			.map(STATUS, assetStatus.status)
			.map(RECORD_STATUS, assetStatus.recordStatus)
			.map(CREATED_DATE, assetStatus.createdDate)
			.map(CREATED_BY, assetStatus.createdBy)
			.map(LAST_MODIFIED_DATE, assetStatus.lastModifiedDate)
			.map(LAST_MODIFIED_BY, assetStatus.lastModifiedBy);
        
        applyPagination(pageable, query, mapping);
        applyOrderById(query);
        
        return loadPage(query, pageable, assetStatus);
    }
    
}
