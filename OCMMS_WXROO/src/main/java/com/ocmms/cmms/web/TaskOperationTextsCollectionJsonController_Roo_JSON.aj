// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.ocmms.cmms.web;

import com.ocmms.cmms.model.pm.task.TaskOperationText;
import com.ocmms.cmms.service.api.TaskOperationTextService;
import com.ocmms.cmms.web.TaskOperationTextsCollectionJsonController;
import com.ocmms.cmms.web.TaskOperationTextsItemJsonController;
import io.springlets.data.domain.GlobalSearch;
import java.util.Collection;
import javax.validation.Valid;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.PutMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.servlet.mvc.method.annotation.MvcUriComponentsBuilder;
import org.springframework.web.util.UriComponents;

privileged aspect TaskOperationTextsCollectionJsonController_Roo_JSON {
    
    declare @type: TaskOperationTextsCollectionJsonController: @RestController;
    
    declare @type: TaskOperationTextsCollectionJsonController: @RequestMapping(value = "/api/taskoperationtexts", name = "TaskOperationTextsCollectionJsonController", produces = MediaType.APPLICATION_JSON_VALUE);
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    private TaskOperationTextService TaskOperationTextsCollectionJsonController.taskOperationTextService;
    
    /**
     * TODO Auto-generated constructor documentation
     * 
     * @param taskOperationTextService
     */
    @Autowired
    public TaskOperationTextsCollectionJsonController.new(TaskOperationTextService taskOperationTextService) {
        this.taskOperationTextService = taskOperationTextService;
    }

    /**
     * TODO Auto-generated method documentation
     * 
     * @return TaskOperationTextService
     */
    public TaskOperationTextService TaskOperationTextsCollectionJsonController.getTaskOperationTextService() {
        return taskOperationTextService;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param taskOperationTextService
     */
    public void TaskOperationTextsCollectionJsonController.setTaskOperationTextService(TaskOperationTextService taskOperationTextService) {
        this.taskOperationTextService = taskOperationTextService;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param globalSearch
     * @param pageable
     * @return ResponseEntity
     */
    @GetMapping(name = "list")
    public ResponseEntity<Page<TaskOperationText>> TaskOperationTextsCollectionJsonController.list(GlobalSearch globalSearch, Pageable pageable) {
        
        Page<TaskOperationText> taskOperationTexts = getTaskOperationTextService().findAll(globalSearch, pageable);
        return ResponseEntity.ok(taskOperationTexts);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return UriComponents
     */
    public static UriComponents TaskOperationTextsCollectionJsonController.listURI() {
        return MvcUriComponentsBuilder
            .fromMethodCall(
                MvcUriComponentsBuilder.on(TaskOperationTextsCollectionJsonController.class).list(null, null))
            .build().encode();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param taskOperationText
     * @param result
     * @return ResponseEntity
     */
    @PostMapping(name = "create")
    public ResponseEntity<?> TaskOperationTextsCollectionJsonController.create(@Valid @RequestBody TaskOperationText taskOperationText, BindingResult result) {
        
        if (taskOperationText.getId() != null || taskOperationText.getVersion() != null) {        
            return ResponseEntity.status(HttpStatus.CONFLICT).build();
        }
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        TaskOperationText newTaskOperationText = getTaskOperationTextService().save(taskOperationText);
        UriComponents showURI = TaskOperationTextsItemJsonController.showURI(newTaskOperationText);
        
        return ResponseEntity.created(showURI.toUri()).build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param taskOperationTexts
     * @param result
     * @return ResponseEntity
     */
    @PostMapping(value = "/batch", name = "createBatch")
    public ResponseEntity<?> TaskOperationTextsCollectionJsonController.createBatch(@Valid @RequestBody Collection<TaskOperationText> taskOperationTexts, BindingResult result) {
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        getTaskOperationTextService().save(taskOperationTexts);
        
        return ResponseEntity.created(listURI().toUri()).build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param taskOperationTexts
     * @param result
     * @return ResponseEntity
     */
    @PutMapping(value = "/batch", name = "updateBatch")
    public ResponseEntity<?> TaskOperationTextsCollectionJsonController.updateBatch(@Valid @RequestBody Collection<TaskOperationText> taskOperationTexts, BindingResult result) {
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        getTaskOperationTextService().save(taskOperationTexts);
        
        return ResponseEntity.ok().build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param ids
     * @return ResponseEntity
     */
    @DeleteMapping(value = "/batch/{ids}", name = "deleteBatch")
    public ResponseEntity<?> TaskOperationTextsCollectionJsonController.deleteBatch(@PathVariable("ids") Collection<Long> ids) {
        
        getTaskOperationTextService().delete(ids);
        
        return ResponseEntity.ok().build();
    }
    
}
