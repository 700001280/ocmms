// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.ocmms.cmms.web;

import com.ocmms.cmms.model.fico.CostCenter;
import com.ocmms.cmms.service.api.CostCenterService;
import com.ocmms.cmms.web.CostCentersCollectionJsonController;
import com.ocmms.cmms.web.CostCentersItemJsonController;
import io.springlets.data.domain.GlobalSearch;
import java.util.Collection;
import javax.validation.Valid;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.PutMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.servlet.mvc.method.annotation.MvcUriComponentsBuilder;
import org.springframework.web.util.UriComponents;

privileged aspect CostCentersCollectionJsonController_Roo_JSON {
    
    declare @type: CostCentersCollectionJsonController: @RestController;
    
    declare @type: CostCentersCollectionJsonController: @RequestMapping(value = "/api/costcenters", name = "CostCentersCollectionJsonController", produces = MediaType.APPLICATION_JSON_VALUE);
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    private CostCenterService CostCentersCollectionJsonController.costCenterService;
    
    /**
     * TODO Auto-generated constructor documentation
     * 
     * @param costCenterService
     */
    @Autowired
    public CostCentersCollectionJsonController.new(CostCenterService costCenterService) {
        this.costCenterService = costCenterService;
    }

    /**
     * TODO Auto-generated method documentation
     * 
     * @return CostCenterService
     */
    public CostCenterService CostCentersCollectionJsonController.getCostCenterService() {
        return costCenterService;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param costCenterService
     */
    public void CostCentersCollectionJsonController.setCostCenterService(CostCenterService costCenterService) {
        this.costCenterService = costCenterService;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param globalSearch
     * @param pageable
     * @return ResponseEntity
     */
    @GetMapping(name = "list")
    public ResponseEntity<Page<CostCenter>> CostCentersCollectionJsonController.list(GlobalSearch globalSearch, Pageable pageable) {
        
        Page<CostCenter> costCenters = getCostCenterService().findAll(globalSearch, pageable);
        return ResponseEntity.ok(costCenters);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return UriComponents
     */
    public static UriComponents CostCentersCollectionJsonController.listURI() {
        return MvcUriComponentsBuilder
            .fromMethodCall(
                MvcUriComponentsBuilder.on(CostCentersCollectionJsonController.class).list(null, null))
            .build().encode();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param costCenter
     * @param result
     * @return ResponseEntity
     */
    @PostMapping(name = "create")
    public ResponseEntity<?> CostCentersCollectionJsonController.create(@Valid @RequestBody CostCenter costCenter, BindingResult result) {
        
        if (costCenter.getId() != null || costCenter.getVersion() != null) {        
            return ResponseEntity.status(HttpStatus.CONFLICT).build();
        }
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        CostCenter newCostCenter = getCostCenterService().save(costCenter);
        UriComponents showURI = CostCentersItemJsonController.showURI(newCostCenter);
        
        return ResponseEntity.created(showURI.toUri()).build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param costCenters
     * @param result
     * @return ResponseEntity
     */
    @PostMapping(value = "/batch", name = "createBatch")
    public ResponseEntity<?> CostCentersCollectionJsonController.createBatch(@Valid @RequestBody Collection<CostCenter> costCenters, BindingResult result) {
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        getCostCenterService().save(costCenters);
        
        return ResponseEntity.created(listURI().toUri()).build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param costCenters
     * @param result
     * @return ResponseEntity
     */
    @PutMapping(value = "/batch", name = "updateBatch")
    public ResponseEntity<?> CostCentersCollectionJsonController.updateBatch(@Valid @RequestBody Collection<CostCenter> costCenters, BindingResult result) {
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        getCostCenterService().save(costCenters);
        
        return ResponseEntity.ok().build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param ids
     * @return ResponseEntity
     */
    @DeleteMapping(value = "/batch/{ids}", name = "deleteBatch")
    public ResponseEntity<?> CostCentersCollectionJsonController.deleteBatch(@PathVariable("ids") Collection<Long> ids) {
        
        getCostCenterService().delete(ids);
        
        return ResponseEntity.ok().build();
    }
    
}
