// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.ocmms.cmms.web;

import com.ocmms.cmms.model.pm.task.TaskHeader;
import com.ocmms.cmms.service.api.TaskHeaderService;
import com.ocmms.cmms.web.TaskHeadersCollectionJsonController;
import com.ocmms.cmms.web.TaskHeadersItemJsonController;
import io.springlets.data.domain.GlobalSearch;
import java.util.Collection;
import javax.validation.Valid;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.PutMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.servlet.mvc.method.annotation.MvcUriComponentsBuilder;
import org.springframework.web.util.UriComponents;

privileged aspect TaskHeadersCollectionJsonController_Roo_JSON {
    
    declare @type: TaskHeadersCollectionJsonController: @RestController;
    
    declare @type: TaskHeadersCollectionJsonController: @RequestMapping(value = "/api/taskheaders", name = "TaskHeadersCollectionJsonController", produces = MediaType.APPLICATION_JSON_VALUE);
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    private TaskHeaderService TaskHeadersCollectionJsonController.taskHeaderService;
    
    /**
     * TODO Auto-generated constructor documentation
     * 
     * @param taskHeaderService
     */
    @Autowired
    public TaskHeadersCollectionJsonController.new(TaskHeaderService taskHeaderService) {
        this.taskHeaderService = taskHeaderService;
    }

    /**
     * TODO Auto-generated method documentation
     * 
     * @return TaskHeaderService
     */
    public TaskHeaderService TaskHeadersCollectionJsonController.getTaskHeaderService() {
        return taskHeaderService;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param taskHeaderService
     */
    public void TaskHeadersCollectionJsonController.setTaskHeaderService(TaskHeaderService taskHeaderService) {
        this.taskHeaderService = taskHeaderService;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param globalSearch
     * @param pageable
     * @return ResponseEntity
     */
    @GetMapping(name = "list")
    public ResponseEntity<Page<TaskHeader>> TaskHeadersCollectionJsonController.list(GlobalSearch globalSearch, Pageable pageable) {
        
        Page<TaskHeader> taskHeaders = getTaskHeaderService().findAll(globalSearch, pageable);
        return ResponseEntity.ok(taskHeaders);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return UriComponents
     */
    public static UriComponents TaskHeadersCollectionJsonController.listURI() {
        return MvcUriComponentsBuilder
            .fromMethodCall(
                MvcUriComponentsBuilder.on(TaskHeadersCollectionJsonController.class).list(null, null))
            .build().encode();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param taskHeader
     * @param result
     * @return ResponseEntity
     */
    @PostMapping(name = "create")
    public ResponseEntity<?> TaskHeadersCollectionJsonController.create(@Valid @RequestBody TaskHeader taskHeader, BindingResult result) {
        
        if (taskHeader.getId() != null || taskHeader.getVersion() != null) {        
            return ResponseEntity.status(HttpStatus.CONFLICT).build();
        }
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        TaskHeader newTaskHeader = getTaskHeaderService().save(taskHeader);
        UriComponents showURI = TaskHeadersItemJsonController.showURI(newTaskHeader);
        
        return ResponseEntity.created(showURI.toUri()).build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param taskHeaders
     * @param result
     * @return ResponseEntity
     */
    @PostMapping(value = "/batch", name = "createBatch")
    public ResponseEntity<?> TaskHeadersCollectionJsonController.createBatch(@Valid @RequestBody Collection<TaskHeader> taskHeaders, BindingResult result) {
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        getTaskHeaderService().save(taskHeaders);
        
        return ResponseEntity.created(listURI().toUri()).build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param taskHeaders
     * @param result
     * @return ResponseEntity
     */
    @PutMapping(value = "/batch", name = "updateBatch")
    public ResponseEntity<?> TaskHeadersCollectionJsonController.updateBatch(@Valid @RequestBody Collection<TaskHeader> taskHeaders, BindingResult result) {
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        getTaskHeaderService().save(taskHeaders);
        
        return ResponseEntity.ok().build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param ids
     * @return ResponseEntity
     */
    @DeleteMapping(value = "/batch/{ids}", name = "deleteBatch")
    public ResponseEntity<?> TaskHeadersCollectionJsonController.deleteBatch(@PathVariable("ids") Collection<Long> ids) {
        
        getTaskHeaderService().delete(ids);
        
        return ResponseEntity.ok().build();
    }
    
}
