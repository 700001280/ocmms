// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.ocmms.cmms.web;

import com.ocmms.cmms.model.mm.storage.RepairMaterialInstockDetail;
import com.ocmms.cmms.service.api.RepairMaterialInstockDetailService;
import com.ocmms.cmms.web.RepairMaterialInstockDetailsCollectionJsonController;
import com.ocmms.cmms.web.RepairMaterialInstockDetailsItemJsonController;
import io.springlets.data.domain.GlobalSearch;
import java.util.Collection;
import javax.validation.Valid;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.PutMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.servlet.mvc.method.annotation.MvcUriComponentsBuilder;
import org.springframework.web.util.UriComponents;

privileged aspect RepairMaterialInstockDetailsCollectionJsonController_Roo_JSON {
    
    declare @type: RepairMaterialInstockDetailsCollectionJsonController: @RestController;
    
    declare @type: RepairMaterialInstockDetailsCollectionJsonController: @RequestMapping(value = "/api/repairmaterialinstockdetails", name = "RepairMaterialInstockDetailsCollectionJsonController", produces = MediaType.APPLICATION_JSON_VALUE);
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    private RepairMaterialInstockDetailService RepairMaterialInstockDetailsCollectionJsonController.repairMaterialInstockDetailService;
    
    /**
     * TODO Auto-generated constructor documentation
     * 
     * @param repairMaterialInstockDetailService
     */
    @Autowired
    public RepairMaterialInstockDetailsCollectionJsonController.new(RepairMaterialInstockDetailService repairMaterialInstockDetailService) {
        this.repairMaterialInstockDetailService = repairMaterialInstockDetailService;
    }

    /**
     * TODO Auto-generated method documentation
     * 
     * @return RepairMaterialInstockDetailService
     */
    public RepairMaterialInstockDetailService RepairMaterialInstockDetailsCollectionJsonController.getRepairMaterialInstockDetailService() {
        return repairMaterialInstockDetailService;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param repairMaterialInstockDetailService
     */
    public void RepairMaterialInstockDetailsCollectionJsonController.setRepairMaterialInstockDetailService(RepairMaterialInstockDetailService repairMaterialInstockDetailService) {
        this.repairMaterialInstockDetailService = repairMaterialInstockDetailService;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param globalSearch
     * @param pageable
     * @return ResponseEntity
     */
    @GetMapping(name = "list")
    public ResponseEntity<Page<RepairMaterialInstockDetail>> RepairMaterialInstockDetailsCollectionJsonController.list(GlobalSearch globalSearch, Pageable pageable) {
        
        Page<RepairMaterialInstockDetail> repairMaterialInstockDetails = getRepairMaterialInstockDetailService().findAll(globalSearch, pageable);
        return ResponseEntity.ok(repairMaterialInstockDetails);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return UriComponents
     */
    public static UriComponents RepairMaterialInstockDetailsCollectionJsonController.listURI() {
        return MvcUriComponentsBuilder
            .fromMethodCall(
                MvcUriComponentsBuilder.on(RepairMaterialInstockDetailsCollectionJsonController.class).list(null, null))
            .build().encode();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param repairMaterialInstockDetail
     * @param result
     * @return ResponseEntity
     */
    @PostMapping(name = "create")
    public ResponseEntity<?> RepairMaterialInstockDetailsCollectionJsonController.create(@Valid @RequestBody RepairMaterialInstockDetail repairMaterialInstockDetail, BindingResult result) {
        
        if (repairMaterialInstockDetail.getId() != null || repairMaterialInstockDetail.getVersion() != null) {        
            return ResponseEntity.status(HttpStatus.CONFLICT).build();
        }
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        RepairMaterialInstockDetail newRepairMaterialInstockDetail = getRepairMaterialInstockDetailService().save(repairMaterialInstockDetail);
        UriComponents showURI = RepairMaterialInstockDetailsItemJsonController.showURI(newRepairMaterialInstockDetail);
        
        return ResponseEntity.created(showURI.toUri()).build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param repairMaterialInstockDetails
     * @param result
     * @return ResponseEntity
     */
    @PostMapping(value = "/batch", name = "createBatch")
    public ResponseEntity<?> RepairMaterialInstockDetailsCollectionJsonController.createBatch(@Valid @RequestBody Collection<RepairMaterialInstockDetail> repairMaterialInstockDetails, BindingResult result) {
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        getRepairMaterialInstockDetailService().save(repairMaterialInstockDetails);
        
        return ResponseEntity.created(listURI().toUri()).build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param repairMaterialInstockDetails
     * @param result
     * @return ResponseEntity
     */
    @PutMapping(value = "/batch", name = "updateBatch")
    public ResponseEntity<?> RepairMaterialInstockDetailsCollectionJsonController.updateBatch(@Valid @RequestBody Collection<RepairMaterialInstockDetail> repairMaterialInstockDetails, BindingResult result) {
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        getRepairMaterialInstockDetailService().save(repairMaterialInstockDetails);
        
        return ResponseEntity.ok().build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param ids
     * @return ResponseEntity
     */
    @DeleteMapping(value = "/batch/{ids}", name = "deleteBatch")
    public ResponseEntity<?> RepairMaterialInstockDetailsCollectionJsonController.deleteBatch(@PathVariable("ids") Collection<Long> ids) {
        
        getRepairMaterialInstockDetailService().delete(ids);
        
        return ResponseEntity.ok().build();
    }
    
}
