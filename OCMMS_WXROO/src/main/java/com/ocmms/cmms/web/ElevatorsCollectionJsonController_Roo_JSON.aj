// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.ocmms.cmms.web;

import com.ocmms.cmms.model.pm.technicalobject.regulatory.Elevator;
import com.ocmms.cmms.service.api.ElevatorService;
import com.ocmms.cmms.web.ElevatorsCollectionJsonController;
import com.ocmms.cmms.web.ElevatorsItemJsonController;
import io.springlets.data.domain.GlobalSearch;
import java.util.Collection;
import javax.validation.Valid;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.PutMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.servlet.mvc.method.annotation.MvcUriComponentsBuilder;
import org.springframework.web.util.UriComponents;

privileged aspect ElevatorsCollectionJsonController_Roo_JSON {
    
    declare @type: ElevatorsCollectionJsonController: @RestController;
    
    declare @type: ElevatorsCollectionJsonController: @RequestMapping(value = "/api/elevators", name = "ElevatorsCollectionJsonController", produces = MediaType.APPLICATION_JSON_VALUE);
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    private ElevatorService ElevatorsCollectionJsonController.elevatorService;
    
    /**
     * TODO Auto-generated constructor documentation
     * 
     * @param elevatorService
     */
    @Autowired
    public ElevatorsCollectionJsonController.new(ElevatorService elevatorService) {
        this.elevatorService = elevatorService;
    }

    /**
     * TODO Auto-generated method documentation
     * 
     * @return ElevatorService
     */
    public ElevatorService ElevatorsCollectionJsonController.getElevatorService() {
        return elevatorService;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param elevatorService
     */
    public void ElevatorsCollectionJsonController.setElevatorService(ElevatorService elevatorService) {
        this.elevatorService = elevatorService;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param globalSearch
     * @param pageable
     * @return ResponseEntity
     */
    @GetMapping(name = "list")
    public ResponseEntity<Page<Elevator>> ElevatorsCollectionJsonController.list(GlobalSearch globalSearch, Pageable pageable) {
        
        Page<Elevator> elevators = getElevatorService().findAll(globalSearch, pageable);
        return ResponseEntity.ok(elevators);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return UriComponents
     */
    public static UriComponents ElevatorsCollectionJsonController.listURI() {
        return MvcUriComponentsBuilder
            .fromMethodCall(
                MvcUriComponentsBuilder.on(ElevatorsCollectionJsonController.class).list(null, null))
            .build().encode();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param elevator
     * @param result
     * @return ResponseEntity
     */
    @PostMapping(name = "create")
    public ResponseEntity<?> ElevatorsCollectionJsonController.create(@Valid @RequestBody Elevator elevator, BindingResult result) {
        
        if (elevator.getId() != null || elevator.getVersion() != null) {        
            return ResponseEntity.status(HttpStatus.CONFLICT).build();
        }
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        Elevator newElevator = getElevatorService().save(elevator);
        UriComponents showURI = ElevatorsItemJsonController.showURI(newElevator);
        
        return ResponseEntity.created(showURI.toUri()).build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param elevators
     * @param result
     * @return ResponseEntity
     */
    @PostMapping(value = "/batch", name = "createBatch")
    public ResponseEntity<?> ElevatorsCollectionJsonController.createBatch(@Valid @RequestBody Collection<Elevator> elevators, BindingResult result) {
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        getElevatorService().save(elevators);
        
        return ResponseEntity.created(listURI().toUri()).build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param elevators
     * @param result
     * @return ResponseEntity
     */
    @PutMapping(value = "/batch", name = "updateBatch")
    public ResponseEntity<?> ElevatorsCollectionJsonController.updateBatch(@Valid @RequestBody Collection<Elevator> elevators, BindingResult result) {
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        getElevatorService().save(elevators);
        
        return ResponseEntity.ok().build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param ids
     * @return ResponseEntity
     */
    @DeleteMapping(value = "/batch/{ids}", name = "deleteBatch")
    public ResponseEntity<?> ElevatorsCollectionJsonController.deleteBatch(@PathVariable("ids") Collection<Long> ids) {
        
        getElevatorService().delete(ids);
        
        return ResponseEntity.ok().build();
    }
    
}
