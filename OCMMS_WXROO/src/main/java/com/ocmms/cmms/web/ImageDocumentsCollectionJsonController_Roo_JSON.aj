// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.ocmms.cmms.web;

import com.ocmms.cmms.model.edm.ImageDocument;
import com.ocmms.cmms.service.api.ImageDocumentService;
import com.ocmms.cmms.web.ImageDocumentsCollectionJsonController;
import com.ocmms.cmms.web.ImageDocumentsItemJsonController;
import io.springlets.data.domain.GlobalSearch;
import java.util.Collection;
import javax.validation.Valid;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.PutMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.servlet.mvc.method.annotation.MvcUriComponentsBuilder;
import org.springframework.web.util.UriComponents;

privileged aspect ImageDocumentsCollectionJsonController_Roo_JSON {
    
    declare @type: ImageDocumentsCollectionJsonController: @RestController;
    
    declare @type: ImageDocumentsCollectionJsonController: @RequestMapping(value = "/api/imagedocuments", name = "ImageDocumentsCollectionJsonController", produces = MediaType.APPLICATION_JSON_VALUE);
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    private ImageDocumentService ImageDocumentsCollectionJsonController.imageDocumentService;
    
    /**
     * TODO Auto-generated constructor documentation
     * 
     * @param imageDocumentService
     */
    @Autowired
    public ImageDocumentsCollectionJsonController.new(ImageDocumentService imageDocumentService) {
        this.imageDocumentService = imageDocumentService;
    }

    /**
     * TODO Auto-generated method documentation
     * 
     * @return ImageDocumentService
     */
    public ImageDocumentService ImageDocumentsCollectionJsonController.getImageDocumentService() {
        return imageDocumentService;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param imageDocumentService
     */
    public void ImageDocumentsCollectionJsonController.setImageDocumentService(ImageDocumentService imageDocumentService) {
        this.imageDocumentService = imageDocumentService;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param globalSearch
     * @param pageable
     * @return ResponseEntity
     */
    @GetMapping(name = "list")
    public ResponseEntity<Page<ImageDocument>> ImageDocumentsCollectionJsonController.list(GlobalSearch globalSearch, Pageable pageable) {
        
        Page<ImageDocument> imageDocuments = getImageDocumentService().findAll(globalSearch, pageable);
        return ResponseEntity.ok(imageDocuments);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return UriComponents
     */
    public static UriComponents ImageDocumentsCollectionJsonController.listURI() {
        return MvcUriComponentsBuilder
            .fromMethodCall(
                MvcUriComponentsBuilder.on(ImageDocumentsCollectionJsonController.class).list(null, null))
            .build().encode();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param imageDocument
     * @param result
     * @return ResponseEntity
     */
    @PostMapping(name = "create")
    public ResponseEntity<?> ImageDocumentsCollectionJsonController.create(@Valid @RequestBody ImageDocument imageDocument, BindingResult result) {
        
        if (imageDocument.getId() != null || imageDocument.getVersion() != null) {        
            return ResponseEntity.status(HttpStatus.CONFLICT).build();
        }
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        ImageDocument newImageDocument = getImageDocumentService().save(imageDocument);
        UriComponents showURI = ImageDocumentsItemJsonController.showURI(newImageDocument);
        
        return ResponseEntity.created(showURI.toUri()).build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param imageDocuments
     * @param result
     * @return ResponseEntity
     */
    @PostMapping(value = "/batch", name = "createBatch")
    public ResponseEntity<?> ImageDocumentsCollectionJsonController.createBatch(@Valid @RequestBody Collection<ImageDocument> imageDocuments, BindingResult result) {
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        getImageDocumentService().save(imageDocuments);
        
        return ResponseEntity.created(listURI().toUri()).build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param imageDocuments
     * @param result
     * @return ResponseEntity
     */
    @PutMapping(value = "/batch", name = "updateBatch")
    public ResponseEntity<?> ImageDocumentsCollectionJsonController.updateBatch(@Valid @RequestBody Collection<ImageDocument> imageDocuments, BindingResult result) {
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        getImageDocumentService().save(imageDocuments);
        
        return ResponseEntity.ok().build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param ids
     * @return ResponseEntity
     */
    @DeleteMapping(value = "/batch/{ids}", name = "deleteBatch")
    public ResponseEntity<?> ImageDocumentsCollectionJsonController.deleteBatch(@PathVariable("ids") Collection<Long> ids) {
        
        getImageDocumentService().delete(ids);
        
        return ResponseEntity.ok().build();
    }
    
}
