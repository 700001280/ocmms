// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.ocmms.cmms.web;

import com.ocmms.cmms.model.pm.plan.PlanItem;
import com.ocmms.cmms.service.api.PlanItemService;
import com.ocmms.cmms.web.PlanItemsCollectionJsonController;
import com.ocmms.cmms.web.PlanItemsItemJsonController;
import io.springlets.data.domain.GlobalSearch;
import java.util.Collection;
import javax.validation.Valid;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.PutMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.servlet.mvc.method.annotation.MvcUriComponentsBuilder;
import org.springframework.web.util.UriComponents;

privileged aspect PlanItemsCollectionJsonController_Roo_JSON {
    
    declare @type: PlanItemsCollectionJsonController: @RestController;
    
    declare @type: PlanItemsCollectionJsonController: @RequestMapping(value = "/api/planitems", name = "PlanItemsCollectionJsonController", produces = MediaType.APPLICATION_JSON_VALUE);
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    private PlanItemService PlanItemsCollectionJsonController.planItemService;
    
    /**
     * TODO Auto-generated constructor documentation
     * 
     * @param planItemService
     */
    @Autowired
    public PlanItemsCollectionJsonController.new(PlanItemService planItemService) {
        this.planItemService = planItemService;
    }

    /**
     * TODO Auto-generated method documentation
     * 
     * @return PlanItemService
     */
    public PlanItemService PlanItemsCollectionJsonController.getPlanItemService() {
        return planItemService;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param planItemService
     */
    public void PlanItemsCollectionJsonController.setPlanItemService(PlanItemService planItemService) {
        this.planItemService = planItemService;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param globalSearch
     * @param pageable
     * @return ResponseEntity
     */
    @GetMapping(name = "list")
    public ResponseEntity<Page<PlanItem>> PlanItemsCollectionJsonController.list(GlobalSearch globalSearch, Pageable pageable) {
        
        Page<PlanItem> planItems = getPlanItemService().findAll(globalSearch, pageable);
        return ResponseEntity.ok(planItems);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return UriComponents
     */
    public static UriComponents PlanItemsCollectionJsonController.listURI() {
        return MvcUriComponentsBuilder
            .fromMethodCall(
                MvcUriComponentsBuilder.on(PlanItemsCollectionJsonController.class).list(null, null))
            .build().encode();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param planItem
     * @param result
     * @return ResponseEntity
     */
    @PostMapping(name = "create")
    public ResponseEntity<?> PlanItemsCollectionJsonController.create(@Valid @RequestBody PlanItem planItem, BindingResult result) {
        
        if (planItem.getId() != null || planItem.getVersion() != null) {        
            return ResponseEntity.status(HttpStatus.CONFLICT).build();
        }
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        PlanItem newPlanItem = getPlanItemService().save(planItem);
        UriComponents showURI = PlanItemsItemJsonController.showURI(newPlanItem);
        
        return ResponseEntity.created(showURI.toUri()).build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param planItems
     * @param result
     * @return ResponseEntity
     */
    @PostMapping(value = "/batch", name = "createBatch")
    public ResponseEntity<?> PlanItemsCollectionJsonController.createBatch(@Valid @RequestBody Collection<PlanItem> planItems, BindingResult result) {
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        getPlanItemService().save(planItems);
        
        return ResponseEntity.created(listURI().toUri()).build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param planItems
     * @param result
     * @return ResponseEntity
     */
    @PutMapping(value = "/batch", name = "updateBatch")
    public ResponseEntity<?> PlanItemsCollectionJsonController.updateBatch(@Valid @RequestBody Collection<PlanItem> planItems, BindingResult result) {
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        getPlanItemService().save(planItems);
        
        return ResponseEntity.ok().build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param ids
     * @return ResponseEntity
     */
    @DeleteMapping(value = "/batch/{ids}", name = "deleteBatch")
    public ResponseEntity<?> PlanItemsCollectionJsonController.deleteBatch(@PathVariable("ids") Collection<Long> ids) {
        
        getPlanItemService().delete(ids);
        
        return ResponseEntity.ok().build();
    }
    
}
