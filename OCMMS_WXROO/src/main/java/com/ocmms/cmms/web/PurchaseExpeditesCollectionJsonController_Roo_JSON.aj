// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.ocmms.cmms.web;

import com.ocmms.cmms.model.mm.procurement.PurchaseExpedite;
import com.ocmms.cmms.service.api.PurchaseExpediteService;
import com.ocmms.cmms.web.PurchaseExpeditesCollectionJsonController;
import com.ocmms.cmms.web.PurchaseExpeditesItemJsonController;
import io.springlets.data.domain.GlobalSearch;
import java.util.Collection;
import javax.validation.Valid;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.PutMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.servlet.mvc.method.annotation.MvcUriComponentsBuilder;
import org.springframework.web.util.UriComponents;

privileged aspect PurchaseExpeditesCollectionJsonController_Roo_JSON {
    
    declare @type: PurchaseExpeditesCollectionJsonController: @RestController;
    
    declare @type: PurchaseExpeditesCollectionJsonController: @RequestMapping(value = "/api/purchaseexpedites", name = "PurchaseExpeditesCollectionJsonController", produces = MediaType.APPLICATION_JSON_VALUE);
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    private PurchaseExpediteService PurchaseExpeditesCollectionJsonController.purchaseExpediteService;
    
    /**
     * TODO Auto-generated constructor documentation
     * 
     * @param purchaseExpediteService
     */
    @Autowired
    public PurchaseExpeditesCollectionJsonController.new(PurchaseExpediteService purchaseExpediteService) {
        this.purchaseExpediteService = purchaseExpediteService;
    }

    /**
     * TODO Auto-generated method documentation
     * 
     * @return PurchaseExpediteService
     */
    public PurchaseExpediteService PurchaseExpeditesCollectionJsonController.getPurchaseExpediteService() {
        return purchaseExpediteService;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param purchaseExpediteService
     */
    public void PurchaseExpeditesCollectionJsonController.setPurchaseExpediteService(PurchaseExpediteService purchaseExpediteService) {
        this.purchaseExpediteService = purchaseExpediteService;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param globalSearch
     * @param pageable
     * @return ResponseEntity
     */
    @GetMapping(name = "list")
    public ResponseEntity<Page<PurchaseExpedite>> PurchaseExpeditesCollectionJsonController.list(GlobalSearch globalSearch, Pageable pageable) {
        
        Page<PurchaseExpedite> purchaseExpedites = getPurchaseExpediteService().findAll(globalSearch, pageable);
        return ResponseEntity.ok(purchaseExpedites);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return UriComponents
     */
    public static UriComponents PurchaseExpeditesCollectionJsonController.listURI() {
        return MvcUriComponentsBuilder
            .fromMethodCall(
                MvcUriComponentsBuilder.on(PurchaseExpeditesCollectionJsonController.class).list(null, null))
            .build().encode();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param purchaseExpedite
     * @param result
     * @return ResponseEntity
     */
    @PostMapping(name = "create")
    public ResponseEntity<?> PurchaseExpeditesCollectionJsonController.create(@Valid @RequestBody PurchaseExpedite purchaseExpedite, BindingResult result) {
        
        if (purchaseExpedite.getId() != null || purchaseExpedite.getVersion() != null) {        
            return ResponseEntity.status(HttpStatus.CONFLICT).build();
        }
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        PurchaseExpedite newPurchaseExpedite = getPurchaseExpediteService().save(purchaseExpedite);
        UriComponents showURI = PurchaseExpeditesItemJsonController.showURI(newPurchaseExpedite);
        
        return ResponseEntity.created(showURI.toUri()).build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param purchaseExpedites
     * @param result
     * @return ResponseEntity
     */
    @PostMapping(value = "/batch", name = "createBatch")
    public ResponseEntity<?> PurchaseExpeditesCollectionJsonController.createBatch(@Valid @RequestBody Collection<PurchaseExpedite> purchaseExpedites, BindingResult result) {
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        getPurchaseExpediteService().save(purchaseExpedites);
        
        return ResponseEntity.created(listURI().toUri()).build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param purchaseExpedites
     * @param result
     * @return ResponseEntity
     */
    @PutMapping(value = "/batch", name = "updateBatch")
    public ResponseEntity<?> PurchaseExpeditesCollectionJsonController.updateBatch(@Valid @RequestBody Collection<PurchaseExpedite> purchaseExpedites, BindingResult result) {
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        getPurchaseExpediteService().save(purchaseExpedites);
        
        return ResponseEntity.ok().build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param ids
     * @return ResponseEntity
     */
    @DeleteMapping(value = "/batch/{ids}", name = "deleteBatch")
    public ResponseEntity<?> PurchaseExpeditesCollectionJsonController.deleteBatch(@PathVariable("ids") Collection<Long> ids) {
        
        getPurchaseExpediteService().delete(ids);
        
        return ResponseEntity.ok().build();
    }
    
}
