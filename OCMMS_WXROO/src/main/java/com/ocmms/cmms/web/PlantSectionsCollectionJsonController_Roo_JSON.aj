// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.ocmms.cmms.web;

import com.ocmms.cmms.model.pm.configuration.PlantSection;
import com.ocmms.cmms.service.api.PlantSectionService;
import com.ocmms.cmms.web.PlantSectionsCollectionJsonController;
import com.ocmms.cmms.web.PlantSectionsItemJsonController;
import io.springlets.data.domain.GlobalSearch;
import java.util.Collection;
import javax.validation.Valid;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.http.ResponseEntity;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.PutMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.servlet.mvc.method.annotation.MvcUriComponentsBuilder;
import org.springframework.web.util.UriComponents;

privileged aspect PlantSectionsCollectionJsonController_Roo_JSON {
    
    declare @type: PlantSectionsCollectionJsonController: @RestController;
    
    declare @type: PlantSectionsCollectionJsonController: @RequestMapping(value = "/api/plantsections", name = "PlantSectionsCollectionJsonController", produces = MediaType.APPLICATION_JSON_VALUE);
    
    /**
     * TODO Auto-generated attribute documentation
     * 
     */
    private PlantSectionService PlantSectionsCollectionJsonController.plantSectionService;
    
    /**
     * TODO Auto-generated constructor documentation
     * 
     * @param plantSectionService
     */
    @Autowired
    public PlantSectionsCollectionJsonController.new(PlantSectionService plantSectionService) {
        this.plantSectionService = plantSectionService;
    }

    /**
     * TODO Auto-generated method documentation
     * 
     * @return PlantSectionService
     */
    public PlantSectionService PlantSectionsCollectionJsonController.getPlantSectionService() {
        return plantSectionService;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param plantSectionService
     */
    public void PlantSectionsCollectionJsonController.setPlantSectionService(PlantSectionService plantSectionService) {
        this.plantSectionService = plantSectionService;
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param globalSearch
     * @param pageable
     * @return ResponseEntity
     */
    @GetMapping(name = "list")
    public ResponseEntity<Page<PlantSection>> PlantSectionsCollectionJsonController.list(GlobalSearch globalSearch, Pageable pageable) {
        
        Page<PlantSection> plantSections = getPlantSectionService().findAll(globalSearch, pageable);
        return ResponseEntity.ok(plantSections);
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @return UriComponents
     */
    public static UriComponents PlantSectionsCollectionJsonController.listURI() {
        return MvcUriComponentsBuilder
            .fromMethodCall(
                MvcUriComponentsBuilder.on(PlantSectionsCollectionJsonController.class).list(null, null))
            .build().encode();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param plantSection
     * @param result
     * @return ResponseEntity
     */
    @PostMapping(name = "create")
    public ResponseEntity<?> PlantSectionsCollectionJsonController.create(@Valid @RequestBody PlantSection plantSection, BindingResult result) {
        
        if (plantSection.getId() != null || plantSection.getVersion() != null) {        
            return ResponseEntity.status(HttpStatus.CONFLICT).build();
        }
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        PlantSection newPlantSection = getPlantSectionService().save(plantSection);
        UriComponents showURI = PlantSectionsItemJsonController.showURI(newPlantSection);
        
        return ResponseEntity.created(showURI.toUri()).build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param plantSections
     * @param result
     * @return ResponseEntity
     */
    @PostMapping(value = "/batch", name = "createBatch")
    public ResponseEntity<?> PlantSectionsCollectionJsonController.createBatch(@Valid @RequestBody Collection<PlantSection> plantSections, BindingResult result) {
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        getPlantSectionService().save(plantSections);
        
        return ResponseEntity.created(listURI().toUri()).build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param plantSections
     * @param result
     * @return ResponseEntity
     */
    @PutMapping(value = "/batch", name = "updateBatch")
    public ResponseEntity<?> PlantSectionsCollectionJsonController.updateBatch(@Valid @RequestBody Collection<PlantSection> plantSections, BindingResult result) {
        
        if (result.hasErrors()) {
            return ResponseEntity.status(HttpStatus.CONFLICT).body(result);
        }
        
        getPlantSectionService().save(plantSections);
        
        return ResponseEntity.ok().build();
    }
    
    /**
     * TODO Auto-generated method documentation
     * 
     * @param ids
     * @return ResponseEntity
     */
    @DeleteMapping(value = "/batch/{ids}", name = "deleteBatch")
    public ResponseEntity<?> PlantSectionsCollectionJsonController.deleteBatch(@PathVariable("ids") Collection<Long> ids) {
        
        getPlantSectionService().delete(ids);
        
        return ResponseEntity.ok().build();
    }
    
}
